angular.module('app.characterSearch', [])

.controller('SearchCharacter', function ($window, $location, Search) {
  var SearchChar = this;


  SearchChar.queryDB = function() {
    // Search.
    //passes th
  }


});